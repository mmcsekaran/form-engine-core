# Form Engine Core

## RFC: Form Engine
The RFC document serves as the source of truth for all implementations in this repository. Any behavior not explicitly addressed in the RFC will require an update to the RFC before proceeding with the implementation.

Ensure that all PRs comply with the rules defined in the RFC.